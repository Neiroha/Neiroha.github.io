# Neiroha Development Archive — 2026-04-10

## Project Overview

**Neiroha** is a Flutter-based AI audio middleware and dubbing workstation.
- Platform: Windows / Android (responsive layout)
- Flutter 3.41.6 / Dart 3.11.4
- Local SQLite via Drift (schema v6), Riverpod for state

---

## Tech Stack

| Layer | Package |
|---|---|
| UI | Flutter Material 3 |
| State | flutter_riverpod ^2.6.1 |
| Database | drift ^2.25.0 + sqlite3_flutter_libs |
| Local server | shelf + shelf_router |
| HTTP client | dio ^5.7.0 |
| Data classes | freezed + json_annotation |
| Audio playback | audioplayers ^6.0.0 |
| File picker | file_picker ^8.0.0 |
| Utilities | uuid, path_provider, path |

---

## Architecture

### Adapter Pattern (`lib/data/adapters/`)
- `TtsAdapter` — abstract base: `synthesize(TtsRequest)`, `healthCheck()`, `getSpeakers()`
- `OpenAiCompatibleAdapter` — `POST /audio/speech` passthrough; speakers via `GET /audio/voices` then fallback `GET /speakers`
- `ChatCompletionsTtsAdapter` — `POST /chat/completions` with audio output (MiMo/Qwen style); speakers via `GET /speakers`
- `CosyVoiceAdapter` — native CosyVoice API: `POST /cosyvoice/speech` (JSON), `POST /cosyvoice/speech/upload` (multipart with ref audio); speakers via `GET /speakers`; health via `GET /health`
- `createAdapter(provider, {modelName})` factory — dispatches on `adapterType` (`openaiCompatible`, `chatCompletionsTts`, `cosyvoice`)

### Database Schema (`lib/data/database/tables.dart`) — v6
```
TtsProviders         — upstream provider configs (with `position` for manual ordering, `enabled` defaults false)
ModelBindings        — provider <-> model mappings
VoiceAssets          — named voice characters (with avatarPath, modelName)
VoiceBanks           — named character rosters; only one active at a time
VoiceBankMembers     — junction: bank <-> asset (many-to-many)
TtsJobs              — background job queue (unused UI, reserved)
QuickTtsHistories    — persistent Quick TTS log (with audioDuration)
PhaseTtsProjects     — Phase TTS projects (name, bankId, scriptText)
PhaseTtsSegments     — segments per project (text, voiceAssetId, audioPath, audioDuration)
DialogTtsProjects    — Dialog TTS projects (name, bankId)
DialogTtsLines       — lines per dialog project (text, voiceAssetId, audioPath, audioDuration)
AudioTracks          — single audio samples library (audioPath, refText, refLang, durationSec, sourceType, avatarPath)
```

### Default Data (seeded on first launch / schema upgrade)
- **3 providers**: OpenAI TTS (`openaiCompatible`), Chat Completions TTS (`chatCompletionsTts`), CosyVoice (`cosyvoice`) — all disabled by default, pre-filled with localhost URLs
- **1 voice character**: "Default Voice" (preset voice "alloy" on OpenAI TTS)
- **1 voice bank**: "Default Bank" (active, contains Default Voice)
- **1 dialog TTS project**: "Sample Dialog"
- **1 phase TTS project**: "Sample Narration" (with placeholder script)

### Riverpod Providers (`lib/providers/app_providers.dart`)
- `databaseProvider` — singleton AppDatabase
- `ttsProvidersStreamProvider` — all providers stream (ordered: enabled first, then position)
- `voiceAssetsStreamProvider` — all voice assets stream
- `voiceBanksStreamProvider` / `activeBankStreamProvider` / `bankMembersStreamProvider(bankId)`
- `quickTtsHistoryStreamProvider`
- `phaseTtsProjectsStreamProvider` / `phaseTtsSegmentsStreamProvider(projectId)`
- `dialogTtsProjectsStreamProvider` / `dialogTtsLinesStreamProvider(projectId)`
- `audioTracksStreamProvider` — all audio tracks stream
- `serverRunningProvider` — API server toggle

### Local API Server (`lib/server/api_server.dart`)
- `GET /v1/models` — lists enabled VoiceAssets
- `POST /v1/audio/speech` — looks up voice -> provider -> adapter -> returns audio bytes
- `GET /health` — server health check

---

## Shared Widgets (`lib/presentation/widgets/`)

### ResizableSplitPane (`resizable_split_pane.dart`)
Reusable two-pane layout with draggable divider, used across all split-layout screens.
- **Drag divider** to resize freely; divider highlights purple on hover/drag
- **Collapse fully** by dragging past threshold (80px) — visible pane fills 100% width
- **Restore from collapsed**: edge drag zone (thin strip at collapsed edge) or prominent Back button (top-left) or Escape key
- **Edge drag zone**: when collapsed, a 5px invisible drag handle appears at the collapsed edge; hovering widens it to 8px with purple highlight; dragging it transitions into split mode with the divider following the cursor
- **Back button**: elevated Material button with arrow icon (20px) and "Back" label (14px, semi-bold), 16x10 padding, shadow for visibility
- **Saved fraction**: collapsing saves the current split ratio; expanding restores it
- **Mobile-ready**: supports `startCollapsedLeft` / `startCollapsedRight` for narrow screens

### Sidebar (`sidebar.dart`)
64px icon nav, main tabs on top, Providers + Settings pinned at bottom separated by divider.

---

## Screens (`lib/presentation/screens/`)

### Quick TTS (`quick_tts_screen.dart`)
- ResizableSplitPane (0.3 left): voice selector | history
- Left: scrollable voice list with avatar, name, mode label (from active bank)
- **Auto-selects first voice** in the active bank when entering the screen (no voice pre-selected → first voice chosen automatically)
- **Reactive send button**: text input changes immediately update the generate button's enabled/disabled state (via `TextEditingController` listener), so voice can be selected first, then text typed — or vice versa
- Right: generation history — 3-line card layout per entry:
  - Line 1: character avatar + name + delete button (per-item)
  - Line 2: input text (2-line ellipsis)
  - Line 3: play/stop button + waveform bars (30% width, 20 bars) + mm:ss duration
- Header: "Delete All" button with 2-click confirmation (outline -> red confirm)
- Bottom: text input + generate button
- Auto-plays after generation; detects and stores `audioDuration` via `AudioPlayer.getDuration()`

### Phase TTS (`phase_tts_screen.dart`)
- ResizableSplitPane (0.3 left): project list | script editor + segments
- Left: project list (SQLite), create/delete projects; each project bound to a Voice Bank
- Right: split view — script editor (auto-saved) + segment panel
- Auto Split: splits script by double newlines -> creates `PhaseTtsSegments` in DB
- Each segment has independent voice dropdown (from project's bank)
- Generate All: synthesizes each segment with its assigned voice, saves to `AppSupportDir/phase_tts/{projectId}/`

### Dialog TTS (`dialog_tts_screen.dart`)
- ResizableSplitPane (0.3 left): project list | chat view
- Left: project list (SQLite), each project bound to a Voice Bank
- Right: Telegram-like chat view with redesigned bubbles:
  - **Bubble width** adapts to text length, max 60% of the panel width; long text wraps naturally
  - **Character header**: avatar circle + name above bubble
  - **Audio row** (below text in bubble): play/stop button + waveform bars (30 bars, max 50% of bubble width) + time label
  - **Waveform progress**: during playback, played bars highlight in accent color, unplayed bars remain dimmed
  - **Time label**: shows `mm:ss` when idle; shows `played/total` seconds during playback (e.g. `3/6`)
  - **Playback position tracking** via `AudioPlayer.onPositionChanged` stream
- Input bar: voice picker + text field + send -> appends `DialogTtsLine` to DB
- Generate All: batch synthesizes ungenerated lines, detects duration, saves to `AppSupportDir/dialog_tts/{projectId}/`

### Voice Design (`voice_design_screen.dart`)
- Voice design (Qwen3-style instruction -> TTS) placeholder

### Voice Assets (`voice_asset_screen.dart`)
- ResizableSplitPane (0.6 left): track list | inspector
- Library of single audio tracks for voice cloning reference
- Upload audio: copies to `AppSupportDir/voice_assets/`, auto-detects duration
- Record button: wired but stubbed (microphone capture not yet implemented)
- Track list: avatar, name, source type label, duration badge, ref-text indicator icon
- Inspector: tapable avatar (file picker), play/stop button with filename + duration, editable name/description/reference text/reference language fields, save + delete
- Source types: upload, record, quickTts, phaseTts, dialogTts

### Voice Characters (`voice_character_screen.dart`)
- ResizableSplitPane (0.6 left): character list | inspector
- Create dialog — provider-aware UI:
  - **Speaker-only providers** (`chatCompletionsTts`, `openaiCompatible`): no mode selector, forced `presetVoice`, only speaker dropdown + voice name field
  - **CosyVoice** (`cosyvoice`): 3-mode selector (zero_shot / cross_lingual / instruct), mode-specific fields
  - **Other providers**: generic 3-option mode selector (Preset Voice / Voice Clone / Voice Design)
  - **Avatar picker** in create dialog
- Inspector: avatar (tapable -> file picker), all fields display, enabled toggle
- Edit / Duplicate / Delete actions

### Voice Bank (`voice_bank_screen.dart`)
- ResizableSplitPane (0.35 left): bank list | roster
- Left: bank list — create, activate/deactivate (only one active at a time), duplicate (copies all members), delete
- Right: roster for selected bank — members from `VoiceBankMembers`
- Add Character dialog: search by name, add single (closes) or Add All
- Health Check: calls `adapter.healthCheck()` per member's provider, shows results dialog

### Providers (`provider_screen.dart`)
- ResizableSplitPane (0.35 left): provider list | editor
- Two-pane layout (like CheekyStudio style)
- Left: reorderable list with drag handles, each row has name/adapter label + enable/disable switch
  - 3 built-in providers seeded on first launch (OpenAI TTS, Chat Completions, CosyVoice)
  - "+" button to add custom providers
  - Enabled providers float to top, then sorted by user-set position
- Right: editor for selected provider — name, adapter type dropdown, base URL, API key (with show/hide toggle), default model name
  - **Health Check** button: uses `adapter.healthCheck()` against the in-memory form values (test before saving), shows green "Healthy" / red "Failed" badge with tooltip
  - Save + Delete buttons
- Providers default to **disabled**; user activates after configuring URL/key

### Settings (`settings_screen.dart`)
- Local API server toggle (port display)

---

## Navigation (`lib/presentation/navigation/app_navigation.dart`)

Sidebar icon nav (72px wide) with tabs:

**Main area (top):**
`quickTts | phaseTts | dialogTts | voiceDesign | voiceAssets | voiceCharacters | voiceBank`

**Pinned at bottom:**
`providers | settings`

---

## Audio File Storage Paths

| Feature | Path |
|---|---|
| Quick TTS | `AppSupportDir/quick_tts/{timestamp}.{ext}` |
| Phase TTS | `AppSupportDir/phase_tts/{projectId}/seg_{i}_{timestamp}.{ext}` |
| Dialog TTS | `AppSupportDir/dialog_tts/{projectId}/line_{i}_{timestamp}.{ext}` |
| Voice Assets | `AppSupportDir/voice_assets/{trackId}.{ext}` |
| Avatars | `AppSupportDir/avatars/{assetId}.{ext}` |

---

## CosyVoice Backend Integration

The CosyVoice backend (`localhost:9880`) serves **two API styles** from a single server:

| Style | Base URL | Adapter Type | Use Case |
|---|---|---|---|
| OpenAI-compatible | `http://localhost:9880/v1` | `openaiCompatible` | Drop-in for OpenAI TTS clients; voices at `/v1/audio/voices` |
| CosyVoice Native | `http://localhost:9880` | `cosyvoice` | Full CosyVoice features: zero_shot, cross_lingual, instruct modes |

**Native API endpoints:**
- `GET /health` — health check
- `GET /speakers` — list speakers (SillyTavern format)
- `GET /cosyvoice/profiles` — list local profiles
- `GET /cosyvoice/meta` — capabilities metadata
- `POST /cosyvoice/speech` — JSON synthesis (profile, mode, instruct_text, prompt_text, speed, response_format)
- `POST /cosyvoice/speech/upload` — multipart synthesis with uploaded prompt_audio file (zero_shot)

---

## Known Limitations / TODO

- `TtsJobs` table exists but no background job queue UI yet
- Phase TTS "Import Text" button is not implemented
- GPT-SoVITS, Qwen3 Native adapters — stubs only (`UnimplementedError`)
- Audio waveform trimmer is text-fields only (no visual waveform)
- Windows Developer Mode required for release builds (symlinks)
- `avoid_print` lint in `lib/server/api_server.dart:35` (pre-existing)
- Phase/Dialog TTS: phone layout not yet adapted (currently desktop split-panel only, but ResizableSplitPane supports collapse for future mobile adaptation)
- Quick TTS waveform is static (decorative); no real waveform data from audio
- Voice Asset microphone recording: entry point exists but capture/editor not implemented
- Voice Asset "Add to library" from Quick/Phase/Dialog TTS screens not yet wired
- Default providers use `localhost` URLs — user must update to their actual endpoints

---

## Migration Notes

Schema bumped destructively on each version change (dev mode). Production will need proper migration steps.

Current version: **v6** (added `AudioTracks` table, `position` column on `TtsProviders`, `enabled` default changed to `false`, seeded default data on create/upgrade)

---

## Changelog — 2026-04-10

### Changed
- **Dialog TTS chat bubble redesign** — bubble width now adapts to text length (max 60% of panel); long text wraps naturally instead of stretching full width
- **Dialog TTS audio row** — waveform bars reduced to 30 (from 40), capped at 50% of bubble width; played bars highlight with accent color during playback; time label shows `played/total` seconds during playback (e.g. `3/6`), `mm:ss` when idle
- **Dialog TTS playback position tracking** — `AudioPlayer.onPositionChanged` stream drives real-time waveform progress and time display
- **Quick TTS auto-select first voice** — entering Quick TTS screen automatically selects the first voice in the active bank, eliminating the need to manually pick a voice before typing
- **Quick TTS reactive send button** — `TextEditingController` listener triggers rebuild on every keystroke, so the generate button enables/disables immediately regardless of whether voice or text was entered first

### Fixed
- Quick TTS: send button did not update when user typed text after selecting a voice (missing `setState` on text change)
- Dialog TTS: chat bubbles stretched to full panel width making audio waveform look disproportionately wide on narrow panels
